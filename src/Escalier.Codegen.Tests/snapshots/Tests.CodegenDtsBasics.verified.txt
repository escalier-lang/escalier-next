input: 
        type Point = {x: number, y: number};
        let add = fn (a: number, b: number) => a + b;
        
output:
export type Point = {
  x: number;
  y: number;
};
export declare const add;