import * as React from "react"
type Props = {
  message: string;
};
// @escType - fn (props: Props) -> React.ReactNode
const Comp: (props: Props) => React.ReactNode;
// @escType - React.ReactNode
const comp: React.ReactNode;
